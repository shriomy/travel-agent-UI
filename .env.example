# Supabase project — the same project the backend points at, so the access token
# the browser gets is the one the backend can verify.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Trip agent backend. Must appear in the backend's CORS_ALLOW_ORIGINS.
VITE_BACKEND_URL=http://localhost:8000
